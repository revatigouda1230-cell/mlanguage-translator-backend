# explanation: Web framework
fastapi>=0.110,<1.0
# explanation: ASGI server (with extras for uvloop, httptools)
uvicorn[standard]>=0.29,<1.0

# explanation: NLP toolkit and model runners
transformers>=4.38,<5.0
torch>=2.1,<3.0

# explanation: language detection
langdetect>=1.0.9,<2.0

# explanation: tokenizers used by Marian/mBART/T5 (avoid runtime errors)
sentencepiece>=0.2.0,<0.3
sacremoses>=0.0.53,<0.1